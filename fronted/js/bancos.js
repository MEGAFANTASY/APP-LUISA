// ============ CONFIGURACIÓN ============
const NOMBRE_HOJA = "bancos";

// ============ CARGAR DATOS AL ABRIR LA PÁGINA ============
document.addEventListener("DOMContentLoaded", () => {
    // El botón Sync y menú de navegación se manejan desde acciones.js
    console.log("Módulo Bancos cargado");
});
